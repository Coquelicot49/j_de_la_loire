import React from 'react';
import Header from '../components/header/Header';

const Quiz = () => {
    return (
        <div>
            <Header/>
            <p>Ceci est la page Jeux - Quiz</p>
        </div>
    );
};

export default Quiz;